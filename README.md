# streamwrapped

pip install flask
pip install python-steam-api
pip install python-dotenv

first terminal:
cd backend/src

python infrastructure.py

second terminal:
cd backend

npm install -g nodemon
npm install cors

npm run dev

third terminal:
cd frontend/client

npm run dev
